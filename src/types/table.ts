export interface AirInfo {
  id: string
  firstScrapeDate: string
  lastScrapeDate: string
  location: string
  number: string
  class: string
  startDate: string
  endDate: string
  condition: string
}

export interface AirportInfo {
  id: string
  location: string
  count: number
}
